# Copy to `config/secrets.toml` only for optional OAuth/publishing integrations.
# AI provider credentials are deliberately absent while mock mode is active.

# Optional: OAuth + publishing keys for the web app
GOOGLE_CLIENT_ID = ""
GOOGLE_CLIENT_SECRET = ""
GOOGLE_REDIRECT_URI = ""

TIKTOK_CLIENT_KEY = ""
TIKTOK_CLIENT_SECRET = ""
TIKTOK_REDIRECT_URI = ""
