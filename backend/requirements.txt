# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Video/Audio Processing
ffmpeg-python>=0.2.0
moviepy>=1.0.3,<2.0.0
Pillow>=10.0.0

# Transcription - Local
faster-whisper>=1.0.3
pywhispercpp>=1.0.0
stable-ts>=2.0.0
filelock>=3.20.1

# Transcription - Cloud
openai>=1.0.0
groq>=0.4.0

# Authentication
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
passlib[bcrypt]>=1.7.4

# Database (production-grade)
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.1.0

# Cloud Storage (direct-to-GCS uploads for Cloud Run)
google-cloud-storage>=3.7.0

# HTTP/API
requests>=2.31.0
urllib3>=2.6.0

# Security
secure>=1.0.1

# Tooling baseline (pip-audit hardening; required by some native deps)
setuptools>=78.1.1

# CLI
typer>=0.12.5

# Testing (dev only, but included for simplicity)
pytest>=7.4
pytest-cov>=5.0
