[pytest]
pythonpath = . ..
testpaths = tests
addopts = --cov=app --cov-config=.coveragerc --cov-report=term-missing:skip-covered --cov-fail-under=90
markers =
    slow: marks tests that run the full transcription pipeline on sample media
