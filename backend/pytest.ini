[pytest]
pythonpath = . ..
testpaths = tests
addopts = --cov=app --cov-config=.coveragerc --cov-report=term-missing:skip-covered --cov-report=json:.coverage.json --cov-fail-under=80
markers =
    slow: marks tests that run the full transcription pipeline on sample media
    media_export: runs real FFmpeg export compatibility and visual regression checks
